Implementation of Dijkstra's algorithm on a group of connected nodes in Python

The config files act as the nodes and contain the the necessary link cost connection information to be used by the router
